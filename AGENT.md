# Competition Agent 工作约定

## 基本边界

比赛官方规则是最高约束。规则不清楚或互相矛盾时，必须在 `competition_spec.yaml` 中设置 `requires_human_confirmation: true`，而不是自行假设。

## 每轮实验的最低要求

- 一个可验证的假设和一个父实验（基线可无父实验）；
- 固定的数据版本、配置、随机种子和执行命令；
- 记录指标、曲线、资源消耗、结论和保留/拒绝决策；
- 单次实验尽量只验证一个变量。

## 必须人工审批

- 下载或运行陌生第三方源码；
- 使用外部数据、预训练模型或改变数据划分；
- 启动高成本或长时间 GPU 训练；
- 向竞赛平台提交结果；
- 删除数据、权重或历史实验；
- 合并多个核心模型改动。

## 不可违反的原则

- 原始数据只读；衍生数据写入 `data/interim/` 或 `data/processed/`；
- 外部代码必须记录来源、许可证与固定 commit hash；
- 论文里的数字和结论必须能追溯到 `experiments/` 中的结构化记录；
- Markdown 是报告，JSON/数据库才是事实来源。
