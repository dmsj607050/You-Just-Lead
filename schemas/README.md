# Data contracts

Markdown is derived from structured data. Core contracts will be:

- `competition.py` — rules, data paths, metric, restrictions, unresolved questions;
- `experiment.py` — pre-run manifest and post-run result;
- `paper.py` — paper metadata, code provenance, license, reproduction record;
- `proposal.py` — hypothesis, evidence, cost, risk, and rollback.
