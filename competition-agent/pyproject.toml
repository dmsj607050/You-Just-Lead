[project]
name = "competition-agent"
version = "0.1.0"
description = "Experiment-driven workflow scaffold for machine-learning competitions"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
