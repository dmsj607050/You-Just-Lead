# Experiment ledger

- `manifests/` — immutable pre-run intentions;
- `results/` — structured post-run outcomes;
- `artifacts/` — curves, checkpoints, predictions, error samples, logs;
- root Markdown files — summaries derived from structured records.
