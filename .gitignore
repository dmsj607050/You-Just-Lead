# Competition artefacts and credentials
#
# 这里以前把路径写死成 workspace/current_competition/。多项目之后那是**静默缺陷**：
# 新建项目（workspace/<新 id>/）的官方规则原件、raw 数据、模型权重都不再被忽略，
# 会被 git 收进版本库 —— 竞赛规则明确禁止传播赛事数据，这不能靠"记得别 git add"挡住。
# 所以改成 workspace/*/ 覆盖每个项目。
workspace/*/input/*
!workspace/*/input/.gitkeep
workspace/*/data/raw/*
!workspace/*/data/raw/.gitkeep
workspace/*/data/interim/*
!workspace/*/data/interim/.gitkeep
workspace/*/data/processed/*
!workspace/*/data/processed/.gitkeep
workspace/*/models/*
!workspace/*/models/.gitkeep
workspace/*/experiments/artifacts/*
!workspace/*/experiments/artifacts/.gitkeep
workspace/*/experiments/tracking/*
!workspace/*/experiments/tracking/.gitkeep

# 克隆下来的第三方仓库与复现运行的产物都不进版本库。
# sources/ 里是别人的代码（外层仓库把它当嵌套仓库收录会得到一个取不到内容的 gitlink，
# 而且它本来就不该由我们分发）；runs/ 里是容器跑出来的权重、日志与产物，几十 MB 起步。
# 计划（plans/）与批准记录（approvals/）都很小，而且是人做的决定的证据，照常进版本库。
workspace/*/reproductions/sources/
workspace/*/reproductions/runs/
database/*.db
database/*.sqlite
.env
__pycache__/
.pytest_cache/
.local-agent-build-venv/
/build/
/dist/
/*.spec

# 发布产物与发布清单：输出目录，不进版本库。
# 原本这里**漏了** `/release/`，于是 `release/` 一直是未跟踪而不是被忽略的状态 ——
# 谁也不该靠"记得别 git add"来挡住十几 MB 的安装包。可审计的记录留在 docs/RELEASE_<版本>.md 里
# （版本号 + commit + 产物 SHA-256），那才进版本库。
/release/

# LaTeX build intermediates. Keep the generated source, PDF and evidence map.
paper/generated_xunfei/*.aux
paper/generated_xunfei/*.bbl
paper/generated_xunfei/*.blg
paper/generated_xunfei/*.fdb_latexmk
paper/generated_xunfei/*.fls
paper/generated_xunfei/*.log
paper/generated_xunfei/*.out
paper/generated_xunfei/*.synctex.gz
paper/generated_xunfei/*.xdv
paper/generated_xunfei/preview*.png

# 桌面仪表盘已废弃并从客户端仓库移除（用户 2026-09-24 决定只保留端侧应用），
# 本仓库本来就不保留它。见 docs/architecture.md D7。
