# Pre-run manifest

Each `EXP-xxxx.json` must record the hypothesis, parent experiment, changed files, configuration path, code revision, data version, seed, command, estimated cost, and rollback plan.
