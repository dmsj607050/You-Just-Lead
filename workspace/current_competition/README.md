# Current competition

1. Copy `competition_spec.template.yaml` to `competition_spec.yaml`.
2. Fill it only after reading the official rules.
3. Put original, unmodified materials in `input/`.
4. Write all derived data, reports, models and submission candidates to their corresponding subdirectories.
