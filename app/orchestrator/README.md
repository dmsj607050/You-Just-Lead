# Orchestrator

This module will own the workflow state machine:

`rules → data → baseline → analyze → propose → approval → run → paper`

LangGraph or a simple explicit Python state machine can be introduced here after the experiment ledger is stable.
